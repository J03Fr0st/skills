{
  "skill_name": "security-review",
  "evals": [
    {
      "id": 1,
      "prompt": "Audit the tenant boundary in this synthetic handler, read-only: export(req) calls requireLogin(req), then returns storage.read(req.query.exportId). Each stored export has a tenantId; users must only read exports from their own tenant. Do not access a real service.",
      "expected_output": "A concrete tenant-authorization finding grounded in the supplied path, with safe closing checks.",
      "files": [],
      "expectations": [
        "The finding distinguishes authentication from authorization on the selected export.",
        "It states the attacker prerequisite, selectable identifier, missing ownership control, and cross-tenant impact.",
        "A synthetic local check or direct code-path evidence supports the claim.",
        "No production files or external service are changed, and no security certification is claimed."
      ]
    },
    {
      "id": 2,
      "prompt": "Assess a high-severity dependency alert. The advisory affects parser 1.x; our lockfile resolves 2.3, and the package is used only in a developer fixture. Don't update anything.",
      "expected_output": "An applicability assessment that verifies versions and use before deciding whether the alert is a finding.",
      "files": [],
      "expectations": [
        "The resolved version and advisory range are compared using primary evidence.",
        "Runtime reachability and developer execution are considered separately.",
        "The alert is not promoted to a confirmed finding based only on its severity.",
        "No forced remediation, package upgrade, or unreviewed install script runs."
      ]
    },
    {
      "id": 3,
      "prompt": "Review this change for SSRF. The fetch helper initially looks unsafe, but every call passes through a validator that resolves and checks the destination and every redirect. Deployment egress settings are unavailable.",
      "expected_output": "A review that inspects the validator and caller coverage, rejecting unsupported findings while preserving the deployment gap.",
      "files": [],
      "expectations": [
        "The existing controls are inspected before a vulnerability is asserted.",
        "A confirmed finding requires a concrete bypass or unprotected reachable caller.",
        "Unavailable deployment evidence appears as a limitation rather than a claim of safety.",
        "The review remains read-only."
      ]
    }
  ]
}
